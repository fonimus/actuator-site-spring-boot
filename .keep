mvn release:clean release:prepare -DdevelopmentVersion=1.0.0-SNAPSHOT -DreleaseVersion=0.1.0 -Dtag=0.1.0 && mvn release:clean
git checkout 0.1.0
mvn deploy -P release
git checkout master
git push

<settings>
  <servers>
    <server>
      <id>ossrh</id>
      <username></username>
      <password></password>
    </server>
  </servers>
</settings>
